apply plugin: 'java'

dependencies {
  compile fileTree(dir: 'libs', include: ['*.jar'])

  compileOnly deps.dagger.apt.compiler
  compile deps.dagger.core

  testCompile deps.test.junit
  testCompile deps.test.truth
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"
